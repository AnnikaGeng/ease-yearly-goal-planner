Here is the stuff that should be fixed based on the first version

1.you should add the yearly progress icon on top right of the screen
![yearly progress](image.png)

2.cards that can be drags shouldn't need to be pressed, it can be dragged naturally, just hold the right side, and drag it

3.when drag the card, it can be slightly bigger than the rest, but the card shouldn't be cutted by the layout, it should overflow the layout, to look more livable

4.it's hard to scroll when user scroll on the priority area, improve it

5.add a fixed postion button on the bottom right, to call for AI or add tasks manually
![add](image-1.png)
